**Initial Version**
Building a currency exchange app
Step 1: Create the Basic Version for PLN/USD
Step 2: Swap USD/PLN
Add loop to ask if to continue or break. This case we use a while loop because we do not know how many times we want to iterate

**evolution**
Remove hardcoded exchange rates
We have to get real time data using APIs, for that to work, we need to install requests (to be run in a terminal not console). convert to json format, acceptable format for APIs and allows for structured data (keys and variables)